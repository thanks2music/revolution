{
  "name": "backend-cloud-run-docker-wordpress",
  "version": "1.0.0",
  "private": true,
  "description": "WordPress Backend - Docker + Cloud Run Environment",
  "scripts": {
    "lint:php": "echo \"PHP linting would go here (phpcs, etc.)\"",
    "format": "prettier --write \"**/*.{js,css,scss}\"",
    "prepare": "echo \"Backend environment ready for plugin development\""
  },
  "devDependencies": {
    "eslint": "^9.38.0",
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "browserslist": [
    "defaults"
  ]
}
